# Keeps pinned GitHub Actions up to date. Build dependencies are added with M0.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "📦️ build"
      include: scope
    labels: ["component: build"]
    groups:
      actions:
        patterns: ["*"]
